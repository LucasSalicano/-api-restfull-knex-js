## API RestFull with Knex.js

This is a simple API RestFull using Knex.js to manage the data

### Install the packages
```
npm install
```

### Create the table
```
npx knex migrate:up
```

### Run the seed
```
npx knix seed:run
```